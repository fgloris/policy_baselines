defaults:
  - task: pusht_lowdim
  - _self_

name: train_direction_mag_mlp_lowdim
_target_: diffusion_policy.workspace.train_direction_mag_mlp_lowdim_workspace.TrainDirectionMagMLPLowdimWorkspace

obs_dim: ${task.obs_dim}
action_dim: ${task.action_dim}
keypoint_dim: ${task.keypoint_dim}
task_name: ${task.name}
exp_name: "dir_perbin_mag_mlp512_reg_pred8_exec1"

horizon: 16
n_obs_steps: 2
n_action_steps: 1
pred_action_steps: 8
n_latency_steps: 0
past_action_visible: False
keypoint_visible_rate: 1.0

# Override dataset padding because the policy is trained to predict pred_action_steps
# even though the environment executes only n_action_steps.
task:
  dataset:
    pad_after: ${eval:'${pred_action_steps}-1'}

policy:
  _target_: diffusion_policy.policy.direction_mag_mlp_lowdim_policy.DirectionMagMLPLowdimPolicy

  horizon: ${horizon}
  obs_dim: ${obs_dim}
  action_dim: ${action_dim}
  # Rollout execution length. The environment consumes only this many actions.
  n_action_steps: ${eval:'${n_action_steps}+${n_latency_steps}'}
  # Supervised prediction length. The network still predicts an 8-step chunk.
  pred_action_steps: ${pred_action_steps}
  n_obs_steps: ${n_obs_steps}

  # 512-MLP baseline
  num_dir_bins: 64
  hidden_dim: 512
  depth: 4
  activation: relu
  layer_norm: True
  dropout: 0.08

  # Loss = CE + per-direction log-magnitude SmoothL1 + GT-direction trajectory SmoothL1.
  # Each direction bin has its own magnitude regression head.
  # All losses are computed in normalized action coordinates.
  dir_loss_weight: 1.0
  mag_loss_weight: 1.0
  traj_loss_weight: 1.0
  # Small regularizer for all per-direction magnitude experts.
  # Important because every sample supervises only one of the 64 mag heads.
  mag_all_reg_weight: 1.0e-3
  # Circular soft labels for direction CE. Target bin gets 0.8, k-1 and k+1 get 0.1 each.
  # Set to 0.0 to recover hard CE.
  dir_neighbor_smoothing: 0.1

  # Ignore direction labels when normalized step length is too small.
  dir_eps: 1.0e-2
  # log target is log(1 + ||delta|| / mag_scale) in normalized action space.
  # The network predicts this value separately for each direction bin.
  mag_scale: 5.0e-2
  mag_head_init_bias: -2.0
  oa_step_convention: True

ema:
  _target_: diffusion_policy.model.diffusion.ema_model.EMAModel
  update_after_step: 0
  inv_gamma: 1.0
  power: 0.75
  min_value: 0.0
  max_value: 0.9999

dataloader:
  batch_size: 512
  num_workers: 1
  shuffle: True
  pin_memory: True
  persistent_workers: False

val_dataloader:
  batch_size: 512
  num_workers: 1
  shuffle: False
  pin_memory: True
  persistent_workers: False

optimizer:
  _target_: torch.optim.AdamW
  lr: 3.0e-4
  betas: [0.95, 0.999]
  eps: 1.0e-8
  weight_decay: 1.0e-4

training:
  device: "cuda:0"
  seed: 42
  debug: False
  resume: True
  lr_scheduler: cosine
  lr_warmup_steps: 500
  num_epochs: 20000
  gradient_accumulate_every: 1
  use_ema: True
  rollout_every: 50
  checkpoint_every: 50
  val_every: 1
  sample_every: 5
  max_train_steps: null
  max_val_steps: null
  tqdm_interval_sec: 1.0

logging:
  project: diffusion_policy_debug
  resume: True
  mode: online
  name: ${now:%Y.%m.%d-%H.%M.%S}_${name}_${task_name}
  tags: ["${name}", "${task_name}", "${exp_name}"]
  id: null
  group: null

checkpoint:
  topk:
    monitor_key: test_mean_score
    mode: max
    k: 8
    format_str: 'epoch={epoch:04d}-test_mean_score={test_mean_score:.3f}.ckpt'
  save_last_ckpt: True
  save_last_snapshot: False

multi_run:
  run_dir: data/outputs/${now:%Y.%m.%d}/${now:%H.%M.%S}_${name}_${task_name}
  wandb_name_base: ${now:%Y.%m.%d-%H.%M.%S}_${name}_${task_name}

hydra:
  job:
    override_dirname: ${name}
  run:
    dir: data/outputs/${now:%Y.%m.%d}/${now:%H.%M.%S}_${name}_${task_name}
  sweep:
    dir: data/outputs/${now:%Y.%m.%d}/${now:%H.%M.%S}_${name}_${task_name}
    subdir: ${hydra.job.num}
